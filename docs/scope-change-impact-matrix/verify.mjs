import assert from 'node:assert/strict';import{evaluate}from'./src/core.js';const x=evaluate({baseline:[{id:'a',dependencies:[]}],proposed:[{id:'a',dependencies:[]},{id:'b',dependencies:['a']} ]});assert.equal(x.valid,true);assert.equal(x.matrix.find(x=>x.id==='b').impact,'direct');const y=evaluate({baseline:[],proposed:[{id:'a',dependencies:['b']},{id:'b',dependencies:['a']}]});assert.ok(y.errors.some(e=>e.code==='CYCLE'));console.log('PASS scope matrix 2/2');
